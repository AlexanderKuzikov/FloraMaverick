# 🌸 FloraMaverick

**FloraMaverick** — это автоматизированный Data Ingestion Pipeline для извлечения структурированных данных из фотографий цветочных букетов. Проект назван в честь Large Vision Model `meta-llama/llama-4-maverick-17b-128e-instruct`, которая выступает "движком" машинного зрения.

Пайплайн превращает сырые фотографии из салона (даже с водяными знаками) в чистый, нормализованный JSON, готовый для импорта в базу данных, Excel или интернет-магазин.

---

## ✨ Ключевые особенности (The "Fishing Rod")

- **Strict JSON Schema:** Модель возвращает 100% валидный JSON по жестко заданной схеме.
- **Smart Rate Limiting:** Умный обход лимитов API. Скрипт сам читает HTTP-заголовки `x-ratelimit-reset` и спит ровно столько миллисекунд, сколько нужно, чтобы не получить бан (429).
- **Anti-ECONNRESET:** Устойчивость к обрывам TCP-соединений со стороны балансировщиков (автоматическая Retry-логика).
- **On-the-fly Compression:** Сжатие изображений перед отправкой (через `sharp`) для экономии контекстного окна модели (токенов) и ускорения сети.
- **QA & Testing Tools:** Встроенные утилиты для генерации визуальных коллажей с пагинацией и anti-crop логикой, чтобы QA-инженер мог быстро проводить ревью ответов нейросети (Gold Set Validation).

---

## 🛠 Структура проекта

* `bouquets_prompt.js` — Ядро логики. Содержит System Prompt (жесткие правила флористики, нормализацию названий) и схему JSON.
* `process_bouquets.js` — Главный боевой скрипт. Читает папку с фото, общается с API Groq, дописывает результаты в итоговый JSON.
* `make_qa_collages.js` — Утилита для сборки коллажей с пагинацией (9 фото на лист) без обрезки для проведения визуального QA-аудита.
* `make_pdf_images.js` — Утилита для генерации чистого PDF-файла из картинок (без текстового слоя) для тестирования галлюцинаций LLM в чатах.

---

## 🚀 Установка и запуск

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/AlexanderKuzikov/FloraMaverick.git
   cd FloraMaverick
